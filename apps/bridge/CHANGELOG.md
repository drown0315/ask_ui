# Changelog

## 0.0.3

- Fix global package scrcpy server lookup when pub writes `rootUri` without a
  trailing slash in `package_config.json`.

## 0.0.2

- Fix scrcpy server path resolution when `ask_ui_bridge` is globally activated
  from pub.dev.

## 0.0.1

- Initial commit
