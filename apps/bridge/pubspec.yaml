name: ask_ui_bridge
description: Local bridge server for Ask UI.
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  vm_service: 14.3.1

dev_dependencies:
  file_testkit: 1.0.0
  test: 1.25.8
