name: ios_screen_mvp_server
description: Local capture and control server for the iOS Screen Web MVP.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.7.0
  shelf: ^1.4.2
  shelf_static: ^1.1.3
  shelf_web_socket: ^3.0.0
  vm_service: ^15.0.2
  web_socket_channel: ^3.0.3

dev_dependencies:
  test: ^1.26.2

